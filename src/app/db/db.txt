用户表（User）
用户ID（UserID）
用户名（Username）
密码（Password）
电子邮件（Email）
手机号码（Phone）
创建时间（CreateTime）
产品表（Product）
产品ID（ProductID）
产品名称（ProductName）
产品描述（ProductDescription）
价格（Price）
返佣比例表（CommissionRate）
返佣比例ID（RateID）
返佣对象类型（ObjectType）
返佣对象ID（ObjectID）
产品ID（ProductID）
返佣比例（Rate）
返佣记录表（CommissionRecord）
返佣记录ID（RecordID）
返佣对象ID（ObjectID）
产品ID（ProductID）
返佣金额（Amount）
返佣时间（CommissionTime）
返佣结算表（CommissionSettlement）
结算ID（SettlementID）
结算周期（SettlementPeriod）
返佣对象ID（ObjectID）
结算金额（Amount）
结算时间（SettlementTime）
返佣审核表（CommissionAudit）
审核ID（AuditID）
返佣记录ID（RecordID）
审核状态（AuditStatus）
审核时间（AuditTime）
返佣报表表（CommissionReport）
报表ID（ReportID）
返佣对象ID（ObjectID）
返佣金额（Amount）
报表时间（ReportTime）
团队表（Team）
团队ID（TeamID）
团队名称（TeamName）
团队负责人ID（LeaderID）
创建时间（CreateTime）
成员表（Member）
成员ID（MemberID）
成员姓名（MemberName）
团队ID（TeamID）
成员角色（MemberRole）

客户关系表（CustomerRelation）
关系ID（RelationID）
团队ID（TeamID）
客户ID（CustomerID）
关系类型（RelationType）
层级（Level）
创建时间（CreateTime）

提现记录表（WithdrawalRecord）
提现记录ID（WithdrawalID）
返佣对象ID（ObjectID）
提现金额（Amount）
提现状态（WithdrawalStatus）
提现时间（WithdrawalTime）

订单记录表（OrderRecord）
订单ID（OrderID）
客户ID（CustomerID）
产品ID（ProductID）
订单金额（Amount）
下单时间（OrderTime）